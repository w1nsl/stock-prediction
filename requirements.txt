# Make sure wheel is installed first to help with binary packages
wheel>=0.38.0

# Core dependencies
streamlit
pandas
numpy

# Visualization libraries - keep these separate
matplotlib
seaborn>=0.12.0
plotly

# Machine learning
scikit-learn>=1.0.0
torch>=2.0.0

# Database
psycopg2-binary>=2.9.0  # Use binary version which doesn't require compilation
sqlalchemy>=2.0.0
python-dotenv>=1.0.0

# Financial data
yfinance>=0.2.0

# NLP
transformers>=4.30.0
datasets>=2.14.0

# For debugging
pip>=21.0.0
setuptools>=60.0.0

# Optional dependencies (not required for core functionality)
# apache-airflow>=2.7.0
# apache-airflow-providers-postgres>=5.6.0 